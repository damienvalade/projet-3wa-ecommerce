{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        {% for company in companies %}
            <div>
                <a href="{{ path('company_show', {id: company.id}) }}">{{ company.name }}</a>
            </div>
        {% else %}
            <div>
                No companies
            </div>
        {% endfor %}
    </div>
{% endblock %}
