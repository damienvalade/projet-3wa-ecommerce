{% extends 'base.html.twig' %}

{% block body %}
    {% for company in companies %}
        <div>
            <a href="{{ path('company_show', {id: company.id}) }}"></a>{{ company.name }}
        </div>
    {% else %}
        <div>
            No companies
        </div>
    {% endfor %}
{% endblock %}
